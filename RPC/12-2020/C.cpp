#include <bits/stdc++.h>

using namespace std; 

int main() {
    string time; 

    cin >> time; 

    string hour = time.substr(0,2);
    //int minute = int(time.substr(3,2)); 
    cout << hour; 
    
}
#include <bits/stdc++.h>

using namespace std; 

const int inf = 10000007;

map <int, int> chemic;


int solve(vector<int> ya, int j){

    if (ya.empty()) return 1; 

    int che = ya.p
    return solve()
    
}


int main () {
    ios::sync_with_stdio(0);cin.tie(0);
    int n, m, a[1000006], aux; 
    vector<int> ya; 

    for(int i=0; i<m; i++){
        cin >> aux; 
        cin >> chemic[aux]; 
        ya.push_back(i+1); 
    }

    return 0; 
}
